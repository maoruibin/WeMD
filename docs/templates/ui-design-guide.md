# WeMD UI 设计规范

> 本文档定义了 WeMD 项目的视觉设计规范，所有新功能开发应遵循此规范以确保产品一致性。

---

## 📋 目录

1. [设计原则](#设计原则)
2. [色彩系统](#色彩系统)
3. [字体排版](#字体排版)
4. [间距系统](#间距系统)
5. [圆角与阴影](#圆角与阴影)
6. [组件规范](#组件规范)
7. [状态设计](#状态设计)
8. [响应式设计](#响应式设计)

---

## 🎨 设计原则

### 核心原则
- **简洁优先** - 界面干净，突出内容
- **微信风格** - 保持与微信编辑器的视觉一致性
- **高效操作** - 减少点击，提升编辑效率
- **本地优先** - 强调隐私和本地化

### 视觉风格
- 玻璃态效果 (Glassmorphism)
- 微妙的动画过渡
- 清晰的信息层级

---

## 🌈 色彩系统

### 品牌色

| 名称 | CSS 变量 | 色值 | 用途 |
|------|----------|------|------|
| 微信绿 | `--ui-accent-primary` | #07c160 | 主要强调色、按钮、链接 |
| 微信绿渐变 | `--ui-accent-gradient` | linear-gradient(135deg, #07c160 0%, #05a850 100%) | 强调背景 |

### 中性色 - 背景

| 名称 | CSS 变量 | 色值 | 用途 |
|------|----------|------|------|
| 页面背景 | `--ui-bg-page` | #f8f9fa | 页面底色 |
| 主背景 | `--ui-bg-primary` | #ffffff | 卡片、面板背景 |
| 次要背景 | `--ui-bg-secondary` | #f6f8fb | 次级区域 |
| 第三级背景 | `--ui-bg-tertiary` | #eef2f6 | 嵌套区域 |
| 悬停背景 | `--ui-bg-hover` | #e6f5eb | 可交互项悬停 |

### 中性色 - 文字

| 名称 | CSS 变量 | 色值 | 用途 |
|------|----------|------|------|
| 主要文字 | `--ui-text-primary` | #0f172a | 标题、正文 |
| 次要文字 | `--ui-text-secondary` | #334155 | 说明文字 |
| 第三级文字 | `--ui-text-tertiary` | #64748b | 辅助信息 |

### 中性色 - 边框

| 名称 | CSS 变量 | 色值 | 用途 |
|------|----------|------|------|
| 边框颜色 | `--ui-border-color` | #e2e8f0 | 分割线、边框 |

### 功能色

| 名称 | 色值 | 用途 |
|------|------|------|
| 成功 | #07c160 | 操作成功 |
| 警告 | #ff9800 | 提示警告 |
| 错误 | #ef4444 | 错误提示 |
| 信息 | #3b82f6 | 信息提示 |

### 深色模式

深色模式通过 `data-ui-theme="dark"` 切换，配色参考 VS Code 深色主题：

| 类型 | 亮色模式 | 深色模式 |
|------|----------|----------|
| 页面背景 | #f8f9fa | #1e1e1e |
| 主背景 | #ffffff | #252526 |
| 主要文字 | #0f172a | #cccccc |
| 边框 | #e2e8f0 | #3c3c3c |

---

## ✍️ 字体排版

### 字体族

| 用途 | CSS 变量 | 字体栈 |
|------|----------|--------|
| 正文 | `--ui-font-sans` | -apple-system, SF Pro Text, Segoe UI, Roboto, sans-serif |
| 代码 | `--ui-font-mono` | SF Mono, Monaco, Fira Code, monospace |

### 字体大小

| 名称 | 大小 | 用途 |
|------|------|------|
| xs | 12px | 辅助文字 |
| sm | 14px | 小号文字 |
| base | 16px | 正文 |
| lg | 18px | 小标题 |
| xl | 20px | 标题 |
| 2xl | 24px | 大标题 |
| 3xl | 32px | 页面标题 |

### 字重

| 名称 | 数值 | 用途 |
|------|------|------|
| normal | 400 | 正文 |
| medium | 500 | 强调文字 |
| semibold | 600 | 小标题 |
| bold | 700 | 标题 |

---

## 📐 间距系统

基础单位：8px (`--ui-spacing-unit`)

| 名称 | CSS 变量 | 数值 | 用途 |
|------|----------|------|------|
| xs | `--spacing-xs` | 4px | 紧凑间距 |
| sm | `--spacing-sm` | 8px | 小间距 |
| md | `--spacing-md` | 16px | 常规间距 |
| lg | `--spacing-lg` | 24px | 大间距 |
| xl | `--spacing-xl` | 32px | 区域间距 |
| xxl | `--spacing-xxl` | 40px | 大区域间距 |

---

## 🔲 圆角与阴影

### 圆角

| 名称 | CSS 变量 | 数值 | 用途 |
|------|----------|------|------|
| sm | `--ui-radius-sm` | 8px | 小元素 |
| md | `--ui-radius-md` | 12px | 按钮、卡片 |
| lg | `--ui-radius-lg` | 16px | 面板 |
| xl | `--ui-radius-xl` | 24px | 大卡片 |
| pill | `--ui-radius-pill` | 999px | 标签 |

### 阴影

| 名称 | CSS 变量 | 样式 | 用途 |
|------|----------|------|------|
| sm | `--ui-shadow-sm` | 0 1px 2px rgba(0,0,0,0.05) | 轻微浮起 |
| md | `--ui-shadow-md` | 0 4px 6px rgba(0,0,0,0.1) | 卡片 |
| lg | `--ui-shadow-lg` | 0 10px 15px rgba(0,0,0,0.1) | 模态框 |

### 玻璃态效果

```css
.glass-effect {
  background: rgba(255, 255, 255, 0.75);
  border: 1px solid rgba(255, 255, 255, 0.5);
  box-shadow: 0 4px 20px -8px rgba(0, 0, 0, 0.08);
  backdrop-filter: blur(12px);
}
```

---

## 🧩 组件规范

### 按钮

#### 主要按钮
```css
.btn-primary {
  background: var(--ui-accent-gradient);
  color: white;
  border-radius: var(--ui-radius-md);
  padding: var(--spacing-sm) var(--spacing-md);
  box-shadow: var(--ui-shadow-md);
}
```

#### 次要按钮
```css
.btn-secondary {
  background: transparent;
  border: 1px solid var(--ui-border-color);
  color: var(--ui-text-primary);
}
```

#### 幽灵按钮
```css
.btn-ghost {
  background: transparent;
  color: var(--ui-text-secondary);
}
.btn-ghost:hover {
  background: var(--ui-bg-hover);
}
```

### 输入框

```css
.input {
  background: var(--ui-bg-primary);
  border: 1px solid var(--ui-border-color);
  border-radius: var(--ui-radius-sm);
  padding: var(--spacing-sm) var(--spacing-md);
  font-size: 14px;
}
.input:focus {
  outline: none;
  border-color: var(--ui-accent-primary);
}
```

### 卡片

```css
.card {
  background: var(--ui-bg-primary);
  border-radius: var(--ui-radius-md);
  padding: var(--spacing-md);
  box-shadow: var(--ui-shadow-sm);
}
```

---

## 🎭 状态设计

### 空状态
```
┌─────────────────────────────┐
│                             │
│      [空状态图标]           │
│      暂无内容               │
│      [操作按钮]             │
│                             │
└─────────────────────────────┘
```

### 加载状态
- 使用骨架屏或 Loading 图标
- 加载文字："加载中..."

### 错误状态
```
┌─────────────────────────────┐
│   [错误图标]                │
│   加载失败                  │
│   [重试按钮]                │
└─────────────────────────────┘
```

---

## 📱 响应式设计

### 断点

| 名称 | 宽度 | 设备 |
|------|------|------|
| sm | 640px | 手机横屏 |
| md | 768px | 平板竖屏 |
| lg | 1024px | 平板横屏 |
| xl | 1280px | 桌面 |
| 2xl | 1536px | 大屏 |

### 适配策略
- 移动端：隐藏次要功能，折叠侧边栏
- 平板：调整布局，保持可用性
- 桌面：完整功能展示

---

## 🎬 动画

### 过渡时长

| 名称 | 时长 | 用途 |
|------|------|------|
| fast | 150ms | 小交互 |
| base | 200ms | 常规过渡 |
| slow | 300ms | 复杂动画 |

### 内置动画

```css
@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

@keyframes slideInRight {
  from { transform: translateX(100%); }
  to { transform: translateX(0); }
}
```

---

## 📦 现有组件库

以下组件可直接复用（位于 `apps/web/src/components/`）：

| 组件 | 路径 | 功能 |
|------|------|------|
| Header | `/Header/` | 顶部导航栏 |
| MarkdownEditor | `/Editor/` | Markdown 编辑器 |
| Toolbar | `/Editor/` | 编辑工具栏 |
| FileSidebar | `/Sidebar/` | 文件侧边栏 |
| ThemePanel | `/Theme/` | 主题面板 |
| HistoryPanel | `/History/` | 历史记录面板 |

---

## 🔗 相关文档

- [PRD 模板](./prd-template.md)
- [技术设计模板](./tech-design-template.md)
- [Bug 模板](./bug-template.md)
